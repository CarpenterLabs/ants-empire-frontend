export type AccountSubject = {
  isLoading: boolean;
  accountData: any;
  hasError: null | any;
};

export const defaultDataSubjectAccount = {
  isLoading: false,
  accountData: null,
  hasError: null,
}